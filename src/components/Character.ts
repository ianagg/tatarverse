export class Character {
  public name: string;
  public level: number;
  public type: string;

  constructor(name: string, level: number, type: string) {
    this.name = name;
    this.level = level;
    this.type = type;
  }
}
